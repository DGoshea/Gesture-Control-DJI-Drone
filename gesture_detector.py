# gesture_detector.py
import mediapipe as mp
import numpy as np
import cv2

class GestureDetector:
    def __init__(self,):
      #Define initializer (check docs)
    def detect_hands(self, ):
        # Logic for detecting hands (check mediaPipe docs)
    
    def calibrate(self, camera, ):
        print("Hold your hand palm out towards camera to calibrate...")
        #Logic to Calibrate hand size (check openCV docs)
    
    def classify_gesture(self, results):
        #Longest method (algorithm for determining gesture (check open cv & MediaPipe)

    #More methods?
